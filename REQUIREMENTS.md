# Spoiler-Free AI Reader — MVP Requirements

> 状态：Draft
> 版本：0.1
> 更新日期：2026-07-28

## 1. 产品目标

构建一个网页端小说阅读器，使用户能够：

1. 上传并阅读 EPUB 小说，兼容 TXT；
2. 自动保存阅读进度；
3. 向 AI 询问人物、事件和此前剧情；
4. 确保 AI 只能使用用户已经读过的原文；
5. 查看并跳转到回答所依据的原文。

MVP 的主要验证目标不是功能数量，而是确认下面的闭环成立：

```text
上传小说
→ 自动拆分章节
→ 在线阅读并记录进度
→ 只检索已读内容
→ 生成有引用的回答
→ 从引用跳回原文
```

## 2. 用户与使用场景

### 2.1 目标用户

- 希望在阅读长篇小说时快速回忆人物和事件的读者；
- 不希望搜索或 AI 回答泄露后续剧情的读者；
- 首版为单用户、本地部署或开发环境使用。

### 2.2 核心场景

用户读到小说中途，忘记某个人物此前做过什么。用户向 AI 提问，系统仅根据当前阅读位置之前的内容回答，并附上原文引用。

## 3. MVP 范围

### 3.1 电子书阅读器

#### FR-READER-01 上传小说

- 优先支持上传 `.epub`，兼容 `.txt`；
- EPUB 读取 metadata、spine、EPUB 3 Navigation 或 EPUB 2 NCX；
- EPUB 单文件大小上限为 50 MB，解压后上限为 200 MB；
- TXT 单文件大小上限为 20 MB；
- 文件必须能够解析为文本；
- MVP 优先支持 UTF-8，可识别并明确提示不支持的编码；
- 保存原始文件名、书名、作者和上传时间；
- 用户可以在上传前修改自动推断的书名和作者；

验收标准：

- 合法 TXT 文件可以成功上传；
- 非 TXT、空文件、超过上限或无法解码的文件返回明确错误；
- 重复上传不会静默覆盖已有小说。

#### FR-READER-02 章节识别

- EPUB 章节顺序以 spine 为准，标题优先使用 TOC；
- EPUB 章节保存 `spine_index`、`source_href` 和可用的 `fragment_id`；
- 支持常见中文章节标题，例如“第一章”“第十二回”“楔子”“序章”“尾声”；
- 每个章节保存章节编号、标题、正文和在整本书中的字符偏移；
- 未检测到章节时，将全文保存为一个默认章节；
- 解析结果按照原文顺序保存。

验收标准：

- 章节正文不丢失、不重复；
- 所有章节拼接后与规范化后的原文内容一致；
- 章节编号、起止偏移保持单调递增。

#### FR-READER-03 书架

- 展示已上传小说；
- 显示书名、作者、章节数、处理状态和最近阅读时间；
- 可以打开一本小说继续阅读；
- 可以删除一本小说，删除操作需要二次确认。

#### FR-READER-04 阅读页面

- 展示章节目录和当前章节；
- 支持上一章、下一章和目录跳转；
- 支持亮色、护眼色和暗色主题；
- 支持调整字号和行高；
- 阅读设置保存在浏览器本地；
- 页面刷新后能够恢复阅读位置。

#### FR-READER-05 阅读进度

- 保存当前章节编号和章节内字符偏移；
- 进度更新采用节流或防抖，避免滚动时频繁写库；
- 进度只能前进，普通滚动回看不降低“已读边界”；
- 用户主动执行“重置阅读进度”时才允许降低已读边界；
- 阅读位置与无剧透检索边界分开建模：
  - `display_position`：页面当前显示位置；
  - `furthest_read_position`：用户到达过的最远位置。

验收标准：

- 刷新或重新打开小说后恢复上次显示位置；
- 无剧透检索使用最远已读位置；
- 并发更新不能使最远已读位置倒退。

### 3.2 LangChain RAG

#### FR-RAG-01 文本切块

- 在章节范围内切块，不跨章节；
- MVP 使用按段落优先、固定长度兜底的切块策略；
- 每个 Chunk 必须保存：
  - `novel_id`
  - `chapter_id`
  - `chapter_number`
  - `content`
  - `start_offset`
  - `end_offset`
- Chunk 偏移量必须能够映射回章节原文；
- 默认目标长度暂定为 600–900 个中文字符，并保留少量重叠。

#### FR-RAG-02 Embedding

- 使用可配置的 OpenAI-compatible Embedding 接口；
- 默认向量维度为 1536；
- Embedding 写入 PostgreSQL pgvector；
- 建立适合余弦距离检索的向量索引；
- Embedding 失败时记录失败状态，允许重试。

#### FR-RAG-03 无剧透过滤

任何候选 Chunk 必须满足：

```text
chunk.chapter_number < furthest_read_chapter

或

chunk.chapter_number == furthest_read_chapter
且 chunk.end_offset <= furthest_read_offset
```

要求：

- 过滤必须在数据库查询或候选集合构建阶段执行；
- 禁止先检索全书再仅依靠 Prompt 隐藏后续内容；
- LLM 输入中不能出现越过已读边界的正文；
- 所有检索结果的越界率必须为 0。

#### FR-RAG-04 检索

MVP 分两步交付：

1. 第一阶段：pgvector Dense 检索；
2. 第二阶段：BM25 与 Hybrid 检索。

要求：

- 查询只作用于当前小说；
- 返回 Top-K Chunk、相关性分数和位置元数据；
- Hybrid 检索采用明确、可测试的融合策略；
- 检索逻辑与回答生成逻辑分离。

#### FR-RAG-05 问答

- 使用 LangChain 编排检索和回答；
- 回答只能依据检索证据；
- 不允许使用模型自身记忆补充小说剧情；
- 每个事实性结论应附带至少一个引用；
- 证据不足时回答“不确定”或“当前已读内容中没有足够证据”；
- 默认使用中文回答；
- 保存问题、回答、引用、模型配置和响应耗时。

#### FR-RAG-06 引用与跳转

每条引用应包含：

- 章节编号和章节标题；
- 原文摘录；
- Chunk 起止偏移；
- 可供前端跳转的定位信息。

验收标准：

- 点击引用后打开对应章节；
- 页面滚动并高亮引用原文；
- 引用内容必须与数据库中的章节原文一致。

## 4. 数据模型要求

MVP 至少包含以下实体：

- `novels`
- `chapters`
- `reading_progress`
- `chunks`
- `rag_queries`
- `rag_citations`

关键约束：

- 所有主要实体使用 UUID；
- 删除小说时级联删除章节、Chunk、阅读进度和问答记录；
- 小说解析和 Embedding 状态必须可观察；
- 时间字段使用带时区的 UTC 时间；
- 偏移量统一使用 Python/JavaScript 字符串索引前，需要通过测试确认 Unicode 计数规则；
- 生产代码通过 Alembic 管理 schema，不在应用启动时隐式建表。

## 5. API 范围

MVP 计划提供：

```text
GET    /health
GET    /api/v1/novels
POST   /api/v1/novels
GET    /api/v1/novels/{novel_id}
DELETE /api/v1/novels/{novel_id}
GET    /api/v1/novels/{novel_id}/chapters
GET    /api/v1/novels/{novel_id}/chapters/{chapter_number}
GET    /api/v1/novels/{novel_id}/progress
PUT    /api/v1/novels/{novel_id}/progress
POST   /api/v1/rag/ask
```

所有错误使用一致结构：

```json
{
  "code": "MACHINE_READABLE_CODE",
  "message": "面向用户的错误说明",
  "details": {}
}
```

## 6. 非功能要求

### NFR-01 性能

- 书架 API 在普通本地开发环境下目标响应时间小于 300 ms；
- 已完成索引的小说，RAG 检索目标响应时间小于 1 秒；
- 不含模型生成时间的完整问答编排目标小于 2 秒；
- 上传和索引采用可观察的状态，不要求请求一直阻塞。

### NFR-02 安全与隐私

- API Key 只能通过环境变量提供；
- 不将 API Key、小说原文、Chunk 或 Embedding 提交到 Git；
- 文件名不能直接作为服务器文件路径；
- 上传内容需要限制大小和类型；
- 发送给云模型的文本范围必须在界面和文档中说明。

### NFR-03 可测试性

- 章节解析器必须有单元测试；
- 无剧透边界过滤必须有边界测试；
- 阅读进度只能前进的规则必须有并发或数据库约束测试；
- RAG 检索与 LLM 调用必须能够独立测试；
- CI 最低要求：后端 lint/test、前端 type-check/build。

### NFR-04 可观察性

- 记录上传、解析、切块、Embedding 和问答阶段的结构化日志；
- 错误日志不记录 API Key；
- RAG 查询记录检索数量、过滤数量、模型和耗时；
- 可以追踪一次回答使用了哪些 Chunk。

## 7. 本轮明确不做

- EPUB、PDF、MOBI 等格式；
- OCR；
- 多用户注册、登录和权限系统；
- 云端多租户；
- Redis 和 Celery；
- 完整知识图谱；
- 多 Agent；
- 小说续写；
- 人物关系图；
- 移动端原生应用；
- 笔记、划线和跨设备同步；
- 自动生成封面。

新增以上功能必须先更新本文档，不直接进入实现。

## 8. 技术决策

| 项目 | MVP 决策 |
|---|---|
| 前端 | React + TypeScript + Vite |
| 后端 | FastAPI |
| ORM | SQLAlchemy 2.x async |
| 数据库 | PostgreSQL 16 |
| 向量存储 | pgvector |
| 数据迁移 | Alembic |
| RAG 编排 | LangChain |
| 模型接口 | OpenAI-compatible API |
| 首版文件格式 | EPUB 优先，兼容 UTF-8/GB18030 TXT |
| 用户模式 | 单用户，本地 `reader_key` |

## 9. 交付阶段

### M0：工程骨架

- [x] 前后端目录
- [x] PostgreSQL + pgvector Compose
- [x] SQLAlchemy 基础模型
- [x] Alembic 初始迁移
- [x] FastAPI 与 React 可构建
- [x] 基础 README

### M1：上传与解析

- [x] 上传校验
- [x] 编码处理
- [x] EPUB metadata、spine 与 TOC
- [x] EPUB XHTML 正文清洗
- [x] EPUB 章节源定位
- [x] 章节解析器
- [x] 小说和章节入库
- [x] 解析测试

完成标准：上传一本测试小说后，可以通过 API 获取正确的章节列表和正文。

### M2：阅读器

- [x] 书架
- [x] 阅读页面
- [x] 章节目录
- [x] 阅读设置
- [x] 显示位置保存
- [x] 最远已读边界保存

完成标准：关闭页面后重新打开，可以继续阅读，并且最远已读边界不会倒退。

### M3：Dense RAG

- [ ] Chunk
- [ ] Embedding
- [ ] pgvector 检索
- [ ] 无剧透 SQL 过滤
- [ ] LangChain 回答
- [ ] 证据不足拒答
- [ ] 引用跳转

完成标准：回答包含可跳转引用，且自动测试确认越界 Chunk 数量为 0。

### M4：Hybrid Retrieval

- [ ] PostgreSQL 全文检索或 BM25 方案
- [ ] Dense 与关键词结果融合
- [ ] 检索评测集
- [ ] Hit@K、MRR 和引用准确率

## 10. MVP 完成定义

只有同时满足以下条件，MVP 才算完成：

- 用户能够上传、打开并阅读一本 TXT 小说；
- 章节拆分没有明显内容丢失或重复；
- 阅读进度可以恢复，最远已读边界不会意外倒退；
- 用户能够针对当前小说提问；
- LLM 输入中不存在已读边界之后的原文；
- 回答包含真实、可跳转的引用；
- 证据不足时系统拒绝编造；
- 自动测试覆盖章节解析、进度边界和无剧透过滤；
- 后端检查和测试通过；
- 前端类型检查和生产构建通过；
- README 包含可复现的本地启动步骤。

## 11. 变更控制

开发前按以下规则管理需求：

1. 新功能先判断是否属于第 3 节 MVP 范围；
2. 不在范围内的功能默认进入 Backlog，不立即实现；
3. 修改无剧透边界、数据模型或 API 合同时，必须先更新本文档；
4. 每个开发任务必须关联一个 Requirement ID；
5. 完成任务时同时更新对应里程碑的勾选状态；
6. 出现实现与需求冲突时，以本文档最新确认版本为准；
7. Draft 版本经确认后改为 `Accepted`，并从 `0.1` 升为 `1.0`。

## 12. 待确认事项

- [ ] TXT 最大文件限制是否确定为 20 MB；
- [ ] 中文编码首版是否需要支持 GB18030；
- [ ] Embedding 默认模型和向量维度；
- [ ] Chunk 默认长度和重叠大小；
- [ ] BM25 使用 PostgreSQL 全文检索还是独立检索库；
- [ ] 是否保留完整的 RAG 问答历史；
- [ ] 删除小说是否需要软删除或恢复能力。
